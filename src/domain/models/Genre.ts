/**
 * Represents a genre of media.
 * @typedef {Object} Genre
 * @property {string} id - The unique identifier for the genre.
 * @property {string} name - The name of the genre.
 */
export type Genre = {
    id: string;
    name: string;
};
